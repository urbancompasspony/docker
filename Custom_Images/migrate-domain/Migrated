#!/bin/bash

# Change here to the SAME configurations of Domain to be migrated!
HOSTNAME="atomic-pi"
NAME="dominio"
NETWORKLAN="host"
USERNAME="administrador"

docker run -t -i \
-h $HOSTNAME \
--name $NAME \
--privileged \
--restart=unless-stopped \
-v /etc/localtime:/etc/localtime:ro \
--network $NETWORKLAN \
-v $USERNAME/activedirectory/cache/:/var/cache/samba \
-v $USERNAME/activedirectory/lib/:/var/lib/samba \
-v $USERNAME/activedirectory/log/:/var/log/samba \
-v $USERNAME/activedirectory/etc/:/etc/samba \
-v $USERNAME/activedirectory/run/:/run/samba \
-v $USERNAME/activedirectory/config/hosts:/etc/hosts \
-v $USERNAME/activedirectory/config/krb5.conf:/etc/krb5.conf \
-v $USERNAME/activedirectory/config/resolv.conf:/etc/resolv.conf \
urbancompasspony/migrate-domain:latest

#-v /mnt:/mnt \
#-v $(pwd)/FOLDER:/mnt \
