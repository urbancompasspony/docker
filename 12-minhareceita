WIP

git clone https://github.com/cuducos/minha-receita.git
RENAME .env.sample to .env

docker pull ghcr.io/cuducos/minha-receita:main

docker-compose up -d postgres

# THIS TAKES ETERNITY, HOLY F*
docker-compose run --rm minha-receita download --directory /mnt/data/ --skip

docker-compose up
docker-compose run --rm minha-receita create
docker-compose run --rm minha-receita transform --no-privacy -d /mnt/data/
